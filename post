#DBPYEXECUTE

if not 'id' in GETPARAMS:
	die()
	
if not GETPARAMS['id']:
	die()
	
post_id = int(GETPARAMS['id'][0])

post_path = "_posts/post_%d.markdown" % post_id

print markdown_to_html(read_file(post_path))